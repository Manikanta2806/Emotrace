# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
python-multipart==0.0.9
httpx==0.27.0

# ── HuggingFace Models ────────────────────────────────────────────────────────
# Emotion detection + causal reasoning (Flan-T5) + sentiment
transformers==4.41.0
# Semantic similarity
sentence-transformers==2.7.0
# Required by transformers
tokenizers>=0.19.0
accelerate>=0.29.0
safetensors>=0.4.0

# ── PyTorch (CPU) — swap for torch+cu121 if you have a GPU ───────────────────
torch==2.3.0
torchvision==0.18.0
torchaudio==0.18.0
--index-url https://download.pytorch.org/whl/cpu

# ── Utilities ──────────────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.4.0

# ── Optional: Supabase DB ─────────────────────────────────────────────────────
# supabase==2.4.6
